name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  
  # Firebase dependencies
  firebase_core: ^3.5.0
  firebase_auth: ^5.3.0   # Optional, for authentication
  cloud_firestore: ^5.4.2   # Optional, if using Firestore
  firebase_storage: ^12.3.1  # Optional, if using Firebase Storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
